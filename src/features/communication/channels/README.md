# Communication: Channels (not yet implemented)

Full spec: [`docs/modules/communication/channels.md`](../../../../docs/modules/communication/channels.md).

Status: sidebar entry renders `ComingSoonPage`. No backend routes exist yet.

**Not the same thing as** `supabase/functions/api/channels/` (the `IChannelProvider` plugin abstraction) -- this is the future business module (a workspace's "connect your WhatsApp number" screen) that calls into that abstraction. See the disambiguation note in `docs/architecture.md`.
