# Communication: Inbox (not yet implemented)

Full spec: [`docs/modules/communication/inbox.md`](../../../../docs/modules/communication/inbox.md).

Status: sidebar entry renders `ComingSoonPage`. No backend routes exist yet, and no concrete `IChannelProvider` is registered yet (see `supabase/functions/api/channels/providers/`), so there is nothing to send/receive through until at least one channel is implemented.
