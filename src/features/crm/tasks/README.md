# CRM: Tasks (not yet implemented)

Full spec: [`docs/modules/crm/tasks.md`](../../../../docs/modules/crm/tasks.md).

Status: sidebar entry renders `ComingSoonPage`. No backend routes exist yet.

When implementing, follow the pattern in `src/features/crm/contacts/`: `types/`, `schemas/`, `hooks/`, `components/`, `pages/`, plus a flat `src/api/tasksApi.ts`.
